# UI
streamlit>=1.28.0

# HTTP. Radius talks to the MusicBrainz, ListenBrainz, Wikipedia and (optional)
# Last.fm JSON APIs through this, behind a disk cache.
requests>=2.31.0

# Data Processing
pandas>=2.2.0

# Testing
pytest==7.4.4

# Timezone data (pandas needs this explicitly on some platforms, e.g. Windows)
tzdata==2023.3
