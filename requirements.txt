# UI
streamlit>=1.40.0

# HTTP for the MusicBrainz, ListenBrainz, Wikidata, Deezer, Last.fm and
# Discogs JSON APIs.
requests>=2.31.0

# Result tables and CSV export in the UI.
pandas>=2.2.0

# Testing
pytest>=7.4.4

# Timezone data (pandas needs this explicitly on some platforms, e.g. Windows)
tzdata>=2023.3
